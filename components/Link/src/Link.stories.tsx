import React from 'react';

import Link from '.';
import notes from '../README.md';

export default {
  title: 'Components|Link',
  parameters: { notes }
};

export const BasicUsage = () => <Link />;
